[project]
name = "migration-fleet"
version = "0.1.0"
description = "Cursor Cloud Agent orchestrator for fleet-wide Python modernization campaigns"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
